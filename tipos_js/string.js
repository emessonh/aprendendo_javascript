// string: conjunto de caracteres

    // Usada com "", '' ou `` (utilizada para interpolação)

    // usando múltiplas linhas '+' ou '\'

    // caracter de escape '\', '\n' (nova linha)

    // obs: strings são imutáveis

// métodos 

    /**
     * replace(); substitui uma string por outra
     * 
     * split(); converte a string em um array
     * 
     * slice(começo, fim); retorna uma substring
     * 
     * toLowerCase(); retorna a string toda em minúscula
     * 
     * toUpperCase(); retorna a string toda em maiúscula
     * 
     * trim(); remove os espaços em branco do começo e final
     */



