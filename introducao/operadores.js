/**
 * 1. Operadores de declaração:
 * '=', '+=', '-=', '*=', '/=', '**=', %=
 * 
 * 2. Operadores Aritméticos:
 * '+', '-', '/', '*', '%', '**' (exponenciacao), '--' (operador de decremento), '++' (operador de incremento)
 * 
 * 3. Operadores de comparação:
 * '==', '!=', '>=', '>', '<=', '<', '===' (compara valores e o tipo), '!=='
 * 
 * 4. Operadores lógicos:
 * '&&' and, '||' (ou), '!' (não)
 * 
 * 5. Operadores binários:
 * '&' (and), '|' (or), '^' (xor), '~' (not), (<<) (deslocamento a esquerda), (>>) (deslocamento a direita), (>>>) (deslocamento a direita com 0)
 * 
 * 6. Operador de concatenação:
 *  '+', Junta duas strings
 * 
 * 7. Outros operadores:
 * ',' (retorna o último valor dos operandos)
 * '?:' (Operador ternário, retorna um valor para True e outro para False mediante uma condição)
 * 'delete' (pode deletar um objeto, uma propriedade de determinado objeto ou um item de uma coleção) 
 * 'typeof' (retorna uma string indicando o tipo do dado)
 * 'void' (retorna o tipo undefined para uma expressão)
 * 'in' (retorna True se determinada propriedade existir no objeto)
 * 'instanceof' (retorna True se o objeto especificado tiver um construtor)

 */